<!-- views/sign-up.handlebars -->

<div class="row">
    <div class="col-sm-5 col-sm-offset-5">
        <form action="/login" method="post">
            <legend>entre</legend>

            <!-- form -->
            {{>login-form}}

            <!-- button -->
            <div class='text-right'>
                <button type="submit" class="btn btn-primary">Criar nova conta</button>
            </div>
        </form>
    </div>
</div>